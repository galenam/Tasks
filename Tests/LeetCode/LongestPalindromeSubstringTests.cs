using Tasks.LeetCode;
using Xunit;

namespace Tests.LeetCode;

public sealed class LongestPalindromeSubstringTests
{
    /*
    [Theory]
    [InlineData("babad", "bab")]
    [InlineData("cbbd", "bb")]
    public void Test(string source, string result)
    {
        Assert.Equal(result, LongestPalindromeSubstring.LongestPalindrome(source));
    }
    */
}